/** Media grid + upload flow land in P4-T4.14. Stubbed for the 4.12 tab shell. */
export function MediaTab({ siteId }: { siteId: string }) {
  return (
    <p className="text-sm text-zinc-500" data-site-id={siteId}>
      The media grid and upload flow arrive in Task 4.14.
    </p>
  );
}
