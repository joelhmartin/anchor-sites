2026-05-18 — baseline captured (Task 1.0 + 1.1)
Suite: vitest run
Result: 4 passed, 1 skipped

Tests:
  ✓ tests/smoke/baseline.test.ts
      ✓ GET /healthz returns 200 with ok:true
      ✓ unknown API route returns 404
      ↓ DB pool connects when DATABASE_URL is set (skipped — no DATABASE_URL)
  ✓ tests/smoke/spa.test.ts
      ✓ GET / returns 200 with the SPA root div (Vite middleware)
      ✓ GET /healthz still works while Vite is mounted (route order)

Manual checks (Task 1.1):
  ✓ npm run dev → server listens on http://localhost:3000
  ✓ curl :3000/healthz → 200 {"ok":true,"db":false}
  ✓ curl :3000/ → 200 with Vite-transformed index.html (includes /@vite/client)

The DB-pool test (skipped) is the canary for migration sanity from Task 1.2
onward. When DATABASE_URL is set in the routine env, it must pass before any
task that touches the DB layer (Tasks 1.2, 1.6, 1.7).
