2026-05-18 — baseline captured (Tasks 1.0 through 1.9)
Suite: vitest run (with DATABASE_URL + TEST_DATABASE_URL set)
Result: 80 passed, 0 skipped, 0 failed

Tests:
  ✓ tests/smoke/baseline.test.ts (3)
      GET /healthz, unknown route 404, DB pool connect
  ✓ tests/smoke/spa.test.ts (2)
      GET / via Vite middleware, route-order check
  ✓ tests/smoke/blocks-preview.test.ts (3)
      GET /__blocks/preview form, POST renders SSR, 400 malformed
  ✓ tests/integration/schema.test.ts (6)
      tables, indexes, FK cascade, updated_at trigger, CHECK, up/down
  ✓ tests/integration/seed.test.ts (2)
      idempotency, expected slugs
  ✓ tests/integration/resolveSite.test.ts (9)
      muldoon/demo domain match, unknown 404, /healthz still works,
      port stripping, subdomain → sites.slug fallback,
      positive cache hit, negative cache hit, passThroughOnMiss
  ✓ tests/integration/page-render.test.ts (6)
      muldoon/demo home, 404 in site shell, unknown host passthrough,
      brand tokens differ, draft-not-served
  ✓ tests/integration/admin-pages.test.ts (10)
      401 missing/wrong token, save → revision, invalid props 400,
      unknown type 400, cross-site 404, reverse-chrono list,
      restore non-destructive, cross-page restore 404, rate limit 429
  ✓ src/server/email/send.test.ts (12)
      5 templates render + var substitution, missing vars stay literal,
      stub/dry-run/api modes, Bearer auth + payload shape, non-2xx
  ✓ src/server/routine-state.test.ts (4)
      atomic roundtrip, formatted output, mutator persist, 3-way race
  ✓ src/blocks/registry.test.ts (4)
      register, get, dup detection, listBlocks
  ✓ src/blocks/blocks.test.tsx (13)
      schema validation + SSR + anchor #8 (no font-family) + side-effect registration
  ✓ src/components/BlockRenderer.test.tsx (6)
      empty, unknown, invalid, three-in-order, non-editable, prod-silent

Manual checks:
  ✓ npm run dev → server on http://localhost:3000 (Express + Vite middleware)
  ✓ curl :3000/healthz → 200 {"ok":true,"db":true}
  ✓ curl :3000/__blocks/preview → 200 HTML form
  ✓ curl -X POST :3000/__blocks/preview → SSR HTML with ac-* classes + data-block-* wrappers
  ✓ tsc --noEmit clean

Env required for the full 80-test baseline:
  DATABASE_URL=postgres://anchor:anchor@localhost:5434/anchor_dev
  TEST_DATABASE_URL=postgres://anchor:anchor@localhost:5434/anchor_test
  (Docker container: anchor-sites-postgres-1, host port 5434)

If TEST_DATABASE_URL is unset the integration tests skip cleanly and the
suite drops to ~47 passing (smoke + unit-only — email + routine-state +
block tests still run, since they don't need a DB). The routine should keep both env vars set.

Boot Postgres at the start of any run that touches the DB:
  docker compose up -d postgres

---

2026-05-19 — Phase 2 baseline update (Task 2.9)
Suite: vitest run with vitest.workspace.ts (root + packages/components)
Result: 156 passed, 0 skipped, 0 failed across 26 test files

Breakdown:
  Root renderer suite: 110 (was 114; lost 4 hero/cta-specific tests
    in src/blocks/blocks.test.tsx now covered by the package suite)
  @anchorcorps/components suite: 46 (15 block renders + 8 manifest
    contract + 23 primitives — Button, Card, Accordion, Carousel, +
    cn helper + index entrypoint smoke + build artifacts)

Single command runs both:
  DATABASE_URL=... TEST_DATABASE_URL=... npm test

Boot Postgres first as before.

---

2026-05-19 — Phase 3 baseline update (Task 3.15)
Suite: vitest run with vitest.workspace.ts
Result: 227 passed, 0 skipped, 0 failed across 34 test files

Breakdown:
  Root renderer suite: 168 (was 110 at Phase 2 close; +58 across
    Phase 3 — eviction (5), site-resolve endpoint (5), brand-tokens
    schema (16), brand-tokens migration + admin save acceptance (5),
    media_assets schema (1), pg-boss boot (5), media upload-url +
    complete callback (9), sharp variant job (4), hydration unit
    + integration (8))
  @anchorcorps/components suite: 59 (was 46 at Phase 2 close;
    +13 across Phase 3 — Image block (10), hero-slider migration (3))

Single command runs both:
  DATABASE_URL=... TEST_DATABASE_URL=... npm test

Phase 3 also added pg-boss to the dev DB schema (pgboss.* tables
created automatically by bootJobs on first run). The schema-migration
test counts 5 tables including media_assets.

---

2026-05-20 — Phase 5 baseline update (Task 5.11 — phase complete)
Suite: vitest run with vitest.workspace.ts
Result: 344 passed, 0 skipped, 0 failed across 52 test files

Breakdown:
  Root suite: 285 (was 238 at Phase 4 close; +47 across Phase 5)
    Editor unit (src/editor, node + jsdom):
      editor-smoke (2 — Puck loads in jsdom + version-pin drift guard),
      puck-adapter round-trip (7), zodToPuckFields (8 + humanizeLabel),
      buildPuckConfig (9 incl. override + plugin-block + nested),
      tiptap-field (4, @tiptap mocked), image-field (4, fetch mocked)
    Editor route (src/admin/pages/EditorPage.test.tsx, Puck stubbed): 8
      (load→toPuckData, publish→fromPuckData save, save error, not-found,
       breadcrumb, status toggle, revisions render, restore reloads)
    Backend (admin-pages.ts integration): +5 — single-page GET (3),
      save-with-status (1), invalid-status 400 (1)
  @anchorcorps/components suite: 59 (unchanged in Phase 5)

Notes:
  - @measured/puck 0.20.2 + @tiptap/{react,starter-kit,pm} 3.23.5 added
    (pinned exact). Editor bundles into the admin SPA (admin JS ~2.4MB).
  - Puck-in-jsdom: editor-smoke loads real Puck (with a ResizeObserver
    shim, src/editor/__tests__/puck-jsdom.ts); EditorPage/AdminApp tests
    MOCK Puck (an unshimmed real-Puck load crashes and cascades into a
    Vite dep-scan abort that fails other files' collection — D-036).
  - No browser/e2e on this machine (operator hard rule). Drag/drop,
    custom-field UX, viewport switcher, and preview visual parity are
    operator-verified at studio.localhost:3000.
  - KNOWN FLAKE (pre-existing, not Phase 5): resolveSite.test.ts
    "/healthz" intermittently returns 403 (~1/3 on cold runs now that the
    file set grew) — a singleFork cross-file global-state leak (see
    STATE.json open_followups FLAKE-RESOLVESITE). Recovers on rerun; all
    Phase-5 CI builds were SUCCESS. Recommend a dedicated isolation fix.

Single command runs both:
  DATABASE_URL=... TEST_DATABASE_URL=... npm test

---

2026-05-20 — Phase 4 baseline update (Task 4.16 — phase complete)
Suite: vitest run with vitest.workspace.ts
Result: 297 passed, 0 skipped, 0 failed across 45 test files

Breakdown:
  Root suite: 238 (was 227 at Phase 3 close; +71 across Phase 4)
    Admin API (admin-sites.ts integration): list (3), detail+pages (5),
      media list (4), create site (5), patch + create-page (8)
    Admin host (admin-host unit + page passthrough): 8
    Admin UI (jsdom, @testing-library/react):
      ui primitives (5), admin api lib (6), AdminApp routing (5),
      SitesListPage (3), NewSiteWizard (4), SiteDetailPage (4),
      PagesTab (4), MediaTab (3), SettingsTab (4)
  @anchorcorps/components suite: 59 (unchanged in Phase 4)

Notes:
  - Admin UI tests are jsdom via per-file `// @vitest-environment jsdom`
    + explicit `afterEach(cleanup)` (RTL auto-cleanup wasn't firing
    reliably between tests in a file).
  - No browser/e2e verification on this machine (operator hard rule —
    no Chrome automation). UI is typecheck- + jsdom-tested only.

Single command runs both:
  DATABASE_URL=... TEST_DATABASE_URL=... npm test

---

2026-05-20 — Phase 6 close (Tasks 6.1 through 6.8, AI editing layer)
Suite: vitest run (with DATABASE_URL + TEST_DATABASE_URL set)
Result: 394 passed, 0 skipped, 0 failed across 58 files
        (root 335, @anchorcorps/components 59)
Phase-6 baseline was 344/52; +50 across Phase 6.

New Phase-6 tests (all AI client + fetch mocked — no network, no key, no spend):
  ✓ src/server/ai/client.test.ts (8)
      resolveAiMode stub/dry-run/api; runMessage modes; pinned-model not
      overridable; raisable max_tokens; no client touched in stub/dry-run
  ✓ src/server/ai/catalog.test.ts (4)
      every registered block present; metadata/aiHints from registry;
      jsonSchema valid + round-trips default props; byte-stable (cacheable)
  ✓ src/server/ai/edit-ops.test.ts (14)
      contract defaults + unknown-op reject; each op; start/end/after
      placement; purity; missing-id/bad-placement; applyAndValidate
      accept / unknown-type / invalid-props / apply-stage
  ✓ src/server/ai/diff.test.ts (6)
      added/removed/updated; insert doesn't false-flag moved; real reorder
  ✓ src/server/ai/propose.test.ts (8)
      stub/dry-run sample; tool_use → validated proposal + diff; cached
      system + registered-type enum sent; unknown-type/invalid-props/
      bad-tool-input reject; text-only = no-op
  ✓ tests/integration/ai-edit.test.ts (6)
      401/400/404; 200 dry-run preview does NOT persist; propose→apply
      with source:'ai' + revision; 429 rate limit
  ✓ src/admin/pages/EditorPage.test.tsx (+4 → 12)
      Ask AI: preview diff; Apply saves source:'ai' + editor reloads;
      Reject discards; endpoint error surfaced

Notes:
  - Shared validator extracted to src/blocks/validate.ts — the save route
    and the AI path run ONE validator (admin-pages 19 tests still green).
  - @anthropic-ai/sdk import does NOT break cold collection (tests inject a
    fake client; the real SDK is never constructed in tests).
  - Live AI edits gated on the operator's ANTHROPIC_API_KEY (Secret Manager);
    prod runs stub until then.

Single command runs both:
  DATABASE_URL=... TEST_DATABASE_URL=... npm test

================================================================================
2026-05-21 — Phase 7 close (Tasks 7.1–7.10, template system)
Suite: vitest run (DATABASE_URL + TEST_DATABASE_URL set)
Result: 451 passed, 0 skipped, 0 failed across 66 files
        (root 392, package components 59). +57 over the 394/58 Phase-6 close.
Verified deterministic under a cleared sequencer cache (= CI cold glob order).

New test files this phase:
  ✓ tests/integration/templates-repo.test.ts (8)
      createTemplate (+ordered pages, brand token), getTemplate ordering/null,
      listTemplates kind/status filter + pages_count, archive (+idempotent),
      dup-slug conflict, block-validation rejection persists nothing
  ✓ src/server/templates/schema.test.ts (4, DB-free)
      validateTemplatePages valid/invalid, input defaults + bad-slug, kind enum
  ✓ tests/integration/templates-api.test.ts (11)
      save-as-template (capture all/subset/brand toggle, 401/400/404/409) +
      templates admin API (list/detail/archive, 404/401)
  ✓ tests/integration/materialize-template.test.ts (5)
      empty-site materialize + adopt tokens + import revisions, idempotent
      re-run, no-override existing tokens, slug-collision skip, throws on
      unknown site/template
  ✓ tests/integration/from-template.test.ts (8)
      create site + enqueue + (stubbed worker) pages/tokens land, operator
      brand tokens not overridden, 400 bad slug, 404 unknown template,
      400 page-kind/archived template, 409 dup site slug
  ✓ tests/integration/seed-templates.test.ts (2)
      starter template valid ordered pages; idempotent re-run
  ✓ tests/integration/page-templates.test.ts (9)
      save-page-as-template (kind:page, no tokens, 401/404/409) +
      pages/from-template (default slug/title + import revision, slug override,
      409 collision, 400 site-template misuse, 404 unknown site/template)
  ✓ src/admin/components/SaveAsTemplateDialog.test.tsx (4, jsdom)
  + extended NewSiteWizard.test.tsx (6, jsdom), PagesTab.test.tsx (5, jsdom),
    schema.test.ts (+3 template-table assertions)

Notes:
  - New tables templates + template_pages (D-041); migration has forward +
    rollback; down/up round-trip asserts 7 tables.
  - Materialization is a pg-boss job (D-019/D-042), idempotent (ON CONFLICT
    DO NOTHING + adopt-tokens-if-empty). Media shared by reference (D-043).
  - Captured blocks re-validated through the ONE shared registry validator
    (D-039) — a template can't hold blocks the save path would reject.
  - Studio UI jsdom-tested with fetch mocked (no Puck involved). Visual QA is
    operator-run at studio.localhost:3000.
  - FLAKE: the pre-existing single-fork/shared-DB ordering flake
    (FLAKE-RESOLVESITE class) can now also surface in templates-api under a
    WARM local cache; cold/CI is deterministic. Re-run cold
    (rm -rf node_modules/.vite/vitest) before investigating a non-resolveSite
    integration failure. Isolation-hardening fix remains an open follow-up.

Single command runs all:
  DATABASE_URL=... TEST_DATABASE_URL=... npm test
