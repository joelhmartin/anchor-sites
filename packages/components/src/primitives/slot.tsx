/**
 * Thin re-export of @radix-ui/react-slot so other primitives can import
 * `Slot` from `../primitives/slot.js` without each one taking a direct
 * dependency line on Radix in source.
 */
export { Slot } from "@radix-ui/react-slot";
