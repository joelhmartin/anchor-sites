# Server
PORT=3000
NODE_ENV=development

# Postgres — matches docker-compose.yml defaults
DATABASE_URL=postgres://anchor:anchor@localhost:5434/anchor_dev

# Email — stub in Task 1.0, wired in Task 1.9. Leave blank until then.
RESEND_API_KEY=
RESEND_FROM=builder@anchorcorps.com
